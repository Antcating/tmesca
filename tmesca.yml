generator:
  type: linear | random
  # link_length: 5
  # resume_sessions: yes
parser:
  type: soup | lighting
  info: full | links
  filter:
    - users
    - groups
    - channels
    - bots
    - stickers
  bot_suffix:
    - _bot
    - bot
  slow_mode: 0
output:
  type: telegram | console
  # bot_token: ENTER TOKEN
  # user_id: ENTER USER ID