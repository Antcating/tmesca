generator:
  # type: linear | random
  # link_length: 5
  # restore_sessions: yes
  # save_sessions: no
parser:
  # type: soup | lighting
  # info: full | link
  # filter:
  #   - users
  #   - groups
  #   - channels
  #   - bots
  #   - stickers
  # bot_suffix:
  #   - _bot
  #   - bot
  # slow_mode: 0
output:
  type: telegram | console | none
  filter: everything | basic
  # bot_token: ENTER TOKEN
  # user_id: ENTER USER ID